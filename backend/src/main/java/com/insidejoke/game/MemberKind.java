package com.insidejoke.game;

/** What a room token was issued for. A PLAYER member acts as CAPTAIN while the room says so. */
public enum MemberKind {
    OWNER_SCREEN,
    PLAYER,
    SCREEN,
    AUDIENCE
}
